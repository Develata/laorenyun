# 合成演示交付物

本目录只接收本仓库离线种子和真实浏览器复演的非秘密输出。

- rehearsals.json：三次从 reset 出发的本地实测；第三次模型端点拒绝连接。
- export/：最后一次预置自传/离线HTML/JSON；全是合成素材，不是模型验收，JSON不含音频。
- river.png、biography.png：浏览器截图，无登录URL或秘密。

UUID和生成时间允许不同；复演断言自传正文和编排完全一致。
公网未配置目标，不把本地 healthy 记录当作已上线证明。
