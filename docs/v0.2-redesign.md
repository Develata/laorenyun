# v0.2 体验重构：设计与验收边界

当前状态：RC3 出处与阅读导出已修正；精确 RC2 语料复验2/3发布，第三次审校格式修复超时，NOT YET。未创建v0.2.0 tag；v0.1.0及旧RC2证据不改。Phase2 R2/R3硬件门禁独立pending。

## 已查证的平台边界

固定 DSH 0d1f500。`ui-sidebar` 的 `sidebar.brand.mark/name` 可以替换品牌；`sidebar.panellist` 与 `main` 对接主要页面。`ui-layout` 提供折叠导航，不替换整个 shell。`ui-sidebar-right` 的注册 tab 是采访时的辅助预览，不承载长河主视图。`ui-settings-general` 拥有设置壳，`ui-settings-models` 拥有模型设置；v0.1 profile 同时禁用了两者。恢复设置不等于授予模型工具权限。minimal agent preset 不是主题，不采用。

Workspace 注册表以稳定 ID 为身份，同时要求成员账本与 session header.cwd 匹配。人物档案必须使用此真实成员关系；Session 是一次采访，不是一个人生。不得仅按当前浏览器选择来决定异步任务的归属。

## 交互研究（2026-09-16）

- [Histography](https://www.histography.io/)：总览先于细节，底部时间窗控制范围，密度而非所有文字同时呈现。借鉴年代导航/密度，避免照搬历史事件点阵。
- [TimelineJS 原始文档](https://timeline.knightlab.com/docs/index.html)：事件具有独立叙事内容，时间轴保持方向感。老人云采用独立记忆详情，避免概览下无限堆叠详情。
- [Pudding 响应式实践](https://pudding.cool/process/responsive-scrollytelling/)及[实现方法](https://pudding.cool/process/how-to-implement-scrollytelling/)：正常滚动；移动端需重新组织内容，交互不能依靠 hover；布局与绘图分离。无需为长河引入滚动劫持框架。
- [CMU / Clemente The 21st Hour](https://hcii.cmu.edu/mhci/capstone/2021/clemente-museum/design.html)：人生总览引导进入关键时刻的深入故事。借鉴叙事层级，不复制资产或虚拟世界。

## 信息架构

主要入口：讲故事 / 人生长河 / 我的自传 / 设置。品牌“老人云 · 留下讲述，慢慢成书”。长河占据主要画布；详情为二级页面；列表为显式无障碍模式。表达画像、自传阅读、生成和导出归“我的自传”。

长河沿纵向时间前进、横向缓慢蜿蜒且不自交。真实 path 弧长映射月份；区间截取真实子路径。2–5px/月为初始比例；密集区域聚合、不改变事实日期。漂流湾不分配虚假日期。流动仅表达时间方向，reduced-motion 关闭动画但不改变位置。

## 自传边界变更（替代 ADR0017 第5项的后续方向）

旧 Renderer 限制 keySentence 逐字复制，导致一条记录一段、元数据进入散文。新流水线为固定 manifest → FactAtoms → Narrative Planner → Writer → 独立事实审校 → 最多一次语义修复。事实约束、语言自由；每段可支持多个事实，出处由软件追溯。未知年份不自动生成“年份不详”。家人证言自然归属、争议细节可省略。Persona 只进入 Writer 的语言风格槽。

独立审校是有界模型判断，不是事实真实性证明；确定性检查仍拒绝未知 fact ID、空支持、缺来源及越界输出。审校失败保留前一个已发布自传，不能回退成未经验证的流畅文本。历史 generation 保持旧结构可读，新结果标记 narrative-v2。

## 必须关闭的验收

纯文字原生提交→无媒体 Source→Transcript→抽取→节点→长河；刷新/重启/同档案新采访可见。两档案互不泄漏；旧 singleton 默认档案迁移重启幂等。至少两轮真实截图评审；真实模型自传需连贯自然而非句子拼接。未执行不得声明通过，不提前打 v0.2 tag。

## 已执行验收（2026-09-19）

插件固定 `623d029898c3c95fac2b4bc33bdf679d2e5d8439`，包0.2.0-rc.1；此包版本是候选标识，不代表通过发行验收。DSH固定0d1f500，11239个上游blob核验无修改；1572项打包依赖版本/完整性保持一致。

- 插件54项自动测试、类型检查、构建和格式检查通过；涵盖旧语音、Branch、Conflict、调度器、来源、迁移及新档案/叙事规则。
- 新建3093隔离Compose卷，以真实gpt-5.6-luna原生纯文字提交工厂证言。Source.mediaId=null，rawAsr为空，持久Transcript及extract操作产生节点，graphRevision=1；同档案新采访可读，另一档案不可读；刷新及容器重启后保持。原v0.1纯文字问题未复现，不把它描述为已修复缺陷。
- 品牌、原生折叠侧栏、设置、辅助右栏通过Chromium；正常模式不显示开发探针。语音秘密不回填，模型配置使用原生目录/凭据。
- 两轮截图评审修正了移动端排版、年代导航、区间切换崩溃。360px无横向溢出，reduced-motion可用，200% CSS缩放与键盘可用；不等同于真实手机或浏览器原生缩放认证。
- 三段各500点采样验证弧长比例，最大相对误差约0.00000375，容差0.005。区间为独立子路径，不扩大节点点击区域；漂流湾点击位置固定。
- 合成更正“春天→秋天”经详情、预览、原生草稿和发送：同节点revision1→2，旧版保留、来源数2、graphRevision2、pageerror0。
- 6个事实的完整合成语料成功生成无画像3章、有画像4章，一段包含3个事实；两版事实/节点修订集合相同。画像实际仅1段本人样本，不据此评估统计准确率。
- 离线导出：Markdown1470B、HTML3777B、JSON20410B；file:// Chromium离线打开HTML，0网络请求、0断裂锚点，无内部绝对路径/已知秘密值。仅合成数据。

### 尚未通过的叙事门禁

含家人代述、未知时间、开放/已解决冲突的稀疏合成档案，数次真实生成被审校拒绝。曾发现家人归属被模型审校漏检，现有确定性归属检查阻止此类发布；不能把所有失败归咎于格式。最新复测仍为NARRATIVE_UNSUPPORTED，未发布候选正文，旧有效版本保持可读。更完整语料的成功不能替代该复杂场景。需要继续改善计划/写作对稀疏证言的组织，保持事实验证而非退回逐字拼接。**因此不打v0.2标签、不声明RC验收通过。**

## 截图（全部合成资料）

[总览](images/v0.2/overview-reduced.png) · [中段](images/v0.2/middle.png) · [详情](images/v0.2/detail.png) · [漂流湾](images/v0.2/drifting.png) · [移动端](images/v0.2/mobile-reduced.png) · [自传阅读](images/v0.2/book-reading-final.png)。reduced-motion总览与普通动画具有相同时间位置。

## 复测入口

- `LAORENYUN_REAL_SMOKE=true node scripts/smoke-v02.mjs`：需显式配置私密启动日志路径、隔离origin/输出目录；真实模型消费，勿在普通CI运行。
- `node scripts/smoke-v02.mjs --verify-restart`：在同一输出目录已有证据且实际重启后使用。
- `LAORENYUN_SYNTHETIC_ACCEPTANCE=true node scripts/smoke-v02-experience.mjs`：指定隔离合成档案，验证导航/详情/取消更正/几何/响应式/设置。正向更正的本机浏览器记录补充上述结果。

Phase2 R2 physical microphone: PENDING；R3 physical-human recovery: PENDING。本轮没有真人录音，也未重跑腾讯云计费调用。来源marker对老人历史隐藏，旧序列化身份仍可能进入模型上下文；未修改DSH核心来消除它。无公开镜像/新tag，v0.1许可与二进制分发限制继续适用。


## RC2 最终验收（2026-09-19）

**Narrative blocker technically closed; awaiting final human quality review before v0.2.0 tag.**

### 已观察失败与修复

先只读检查旧隔离环境6个失败generation、15份reviews，实际问题包括归属/身份、时间连接、心理/因果修饰、标题、已解决更正再次进入写作，以及仅检查factRefs但未检查语义覆盖。后续修复过程没有把偶然成功当成通过：ee19f0d组为2/3；9db85c4三本虽发布但人工发现成长地扩张；2d606d5、8336d4e等组还发现谋生标题、时间cue与家人归属问题，均不计验收。完整迭代证据见[插件记录](https://github.com/Develata/dsh-laorenyun/blob/main/docs/v0.2.md)，旧失败没有改写成成功。

最终保留固定manifest→派生FactAtoms→规划→段落Writer→原子审校→最多一次局部语义修复→发布。未改Memory Graph/Workspace/语音/Branch/长河/DSH上游。

- 本人有据事实required，未知日历仍required；家人素材requires_attribution；无身份绑定的关系词optional_ambiguous；开放冲突excluded。child明确指传主子女，绝不是传主小时候。已解决更正只把当前1983年事实送入写作，旧1982年证言仍留在出处历史。
- 每个事实必须使用或合法省略；不强迫不安全素材成文。原子声明只用本段factRefs，检查实际span、角色、时间与实际覆盖；已知日期/童年限定不能传给同句的无日期事实。
- 修复只接收本段原文、brief、允许事实和拒绝声明，不再接收章标题/Persona；稳定E目标由Host确定。被拒绝范围覆盖的片段可改写，其余支持内容保留；修复仍需完整审校/覆盖。格式修复有具体错误反馈但次数/截止不增加。
- 纯主题章名自由；事实型标题还必须来自已验证正文。失败只修标题或使用已支持短句/章序号，不把正文退回keySentence拼接。

### 固定版本三次真实生成

实际测试代码：`fcfc3d7521402bad48327f4a3c1b8f0611a22532`，包`0.2.0-rc.2`；openai-responses / gpt-5.6-luna，正常Docker老人profile，无源码挂载。同一graphRevision=11，10个当前节点、11个修订、11份Source/Transcript，一个open Conflict、一个resolved Conflict。Persona为显式合成风格fixture，不是真人风格质量评估。

| 次数 | Persona | 发布 | 章/段 | 使用/事实总数 | 段落修复 | 标题修复 | 格式修复 | 浏览器总耗时 |
|---|---|---|---|---|---|---|---|---|
| 1 | 无 | 成功 | 2/3 | 7/10 | 0 | 2 | 0 | 93.7s |
| 2 | 有 | 成功 | 2/2 | 7/10 | 1 | 0 | 0 | 101.1s |
| 3 | 有 | 成功 | 2/3 | 7/10 | 1 | 1 | 0 | 105.8s |

三次均省略F001（ambiguous_attribution）、F005/F010（open_conflict）。全部FactAtoms、节点修订和来源集合一致，事实宇宙SHA256为`3d7aa6184b6a37a3c6614513051551e2ad9ab98eee5465156eb43329959296b7`。耗时含队列与浏览器轮询，不是精确的云服务单次延迟。

逐本审读最终正文未发现新增具体事实、身份重绑定、伪日期或任选冲突一方；没有未知日期元数据，也不是一节点一段。初稿仍会犯错：本组标题“后来靠修车吃饭”“手艺是慢慢学会的”和错误“那时候”关系被拒绝后局部修复。第2本“把手艺做成日子”略有修辞，留给作者风格审读。这不是通用准确率证明。

[三本完整合成自传及实际导出](evidence/rc2/README.md) · [逐次计划/审校/修复记录](evidence/rc2/evaluation.json)。第三本Markdown/HTML/JSON逐段FactAtom→节点修订→Transcript→Source共8条引用校验通过；开放冲突与旧修订仍在JSON出处中。HTML实际file://离线Chromium打开：0网络请求、0断裂锚点；没有远程资源、内部绝对路径或已知配置秘密值。

8个真实对抗例子：伪年份/姓名/动机/天气/因果、家人冒充本人、child冒充童年身份均拒绝；普通中性转场接受。测试代码和固定合成fixture由插件拥有，无新ML/运行依赖。

### 验证与复测

插件实际执行`pnpm check`（65项，含旧语音、内存图、Conflict、Branch、scheduler、纯文字/语音提取一致性、档案隔离、长河/更正、导出）、`pnpm format:check`、`git diff --check`。应用执行upstream校验11239个blob零源码修改、packaging-lock校验1572条版本/完整性、配置4项测试、正常固定SHA Compose构建/启动、三次真实浏览器生成/导出/刷新、离线HTML与出处校验。没有重跑实体麦克风或腾讯计费调用，也没有把既有视觉测试冒充本次完整重测。

复测必须新建隔离项目/空卷，不能对真人卷运行seed：

```bash
LAORENYUN_PORT=3094 docker compose -p laorenyun-rc2 build
LAORENYUN_PORT=3094 docker compose -p laorenyun-rc2 run --rm --no-deps \
  -e LAORENYUN_RC2_ACCEPTANCE=true --entrypoint node laorenyun \
  /opt/dsh-laorenyun/lib/rc2-seed.js /app/data --empty-synthetic-volume
LAORENYUN_PORT=3094 docker compose -p laorenyun-rc2 up -d --wait
# 将容器启动日志保存到权限600的本机私密文件，切勿提交或输出访问token。
LAORENYUN_RC2_ACCEPTANCE=true LAORENYUN_PRIVATE_LOG=/private/launch.log \
  LAORENYUN_SMOKE_ORIGIN=http://127.0.0.1:3094 node scripts/smoke-rc2.mjs
```

实际调用使用同流程本机诊断脚本`/tmp/laorenyun-rc2/browser.mjs`，默认三次；提交的脚本移除了单次诊断覆盖选项。真实模型测试不进入普通CI。普通启动不会seed演示资料；现有DB会使seed明确拒绝。

R2 physical microphone: PENDING；R3 physical-human recovery: PENDING。v0.1.0 tag与历史不改；没有v0.2.0 tag或公开镜像发布。最终是否打tag，由作者读完合成书稿后决定。

### 最终文档版本集成与重启

最终插件pin为`a8ab590748235e1dac0f8593e75a5cd59a56a5e3`；相对三次验收的`fcfc3d7521402bad48327f4a3c1b8f0611a22532`仅追加文档。重新构建镜像后逐一比较全部6个运行JS模块（含SQLite worker）SHA256，完全一致，因此不重复计费生成。

最终本机镜像`laorenyun:0.2.0-rc.2`：`sha256:b20d89a181dcb44674ab7d1e75b36f0ee380ba3cc84859bccc4134c6c2a33971`。隔离项目`laorenyun-rc2`使用`laorenyun-rc2_laorenyun-data`卷；用户`10001:10001`、localhost端口3094、health=healthy。实际Compose restart后，新Chromium上下文重新授权打开自传，三本已发布生成仍可查询，章节导航可见。验收结束停止测试容器，保留合成卷供审读。

新增9个合成证据文件扫描：已知私密配置值/内部绝对路径命中0。未修改私密.env、真人卷或任何发行标签。


## RC3 验收（2026-09-19，未通过发布门槛）

**NOT YET — 第三次真实叙事审校格式修复超时，未达到3/3。** RC2成功证据保持原样；此处为新的复验结果。

实现只修改派生出处、标题异常隔离和阅读导出。FactAtom.sourceRefs/testimony 是当前支持，historyRefs保留继承历史。resolved同节点更正使用Conflict修订边界剔除旧支持；没有修改图或删除证言。open Conflict继续省略。标题审查拒绝隐喻中无依据的生计/人生意义；标题审校格式异常复用已有安全标题兜底，正文审查与总/单次时限不放宽。

### 精确语料与结果

原RC2合成卷只读复制到 `laorenyun-rc3-final_laorenyun-data`，nodes/revisions/transcripts/conflicts的内容及ID逐项相同。固定图revision11，10当前记忆、11历史修订、11证言。实际测试代码019a4ff81ee7a9245daf95a8adcb03656d93d53d；后续341091d仅文档。

| 次数 | Persona | 发布 | 章/段 | 使用/省略事实 | 段落/标题修复 | 浏览器耗时 |
|---|---|---|---|---|---|---|
| 1 | 无 | 成功 | 2/2 | 7/3 | 0/0 | 65.013s |
| 2 | 有 | 成功 | 2/3 | 7/3 | 0/0 | 110.332s |
| 3 | 有 | 失败 | 未发布 | 计划相同策略 | 首段审校格式修复超时 | 87.720s |

路由openai-responses/gpt-5.6-luna。第三次 MODEL_TIMEOUT，之前格式校验为review span absent；模型没有在60s单任务时限内完成修复。失败未覆盖已有自传。没有把2/3写作3/3，没有无穷重跑。

两本标题均为“村里的日子”“修理铺以前和以后”。正文未发现新增日期、身份、原因或把子女证言冒充本人记忆。第2本保留原证言的“那时候啊”，未造具体日历。独立标题探针拒绝“把手艺做成日子”的生计/生活方式暗示（10.051s），接受“修理铺”（17.533s）。前置测试脚本的Cordis退出调用错误已改正，两个探针正常退出；不属于产品异常。

### 1982→1983 与导出

- 当前支持：b56a81df-d488-470f-baf0-2f603caaeaa2（1983更正）。
- 历史：仍有56a09431-24ac-441b-9744-6445a39080a6（1982）及更正；图/history接口未改。
- 两本当前Section都不引用旧证言。第2本新导出的Markdown/HTML仅7个已用来源，0未引用脚注，不出现1982旧说法作为支持。
- memories.json保留11证言、11修订及resolved Conflict，完整旧证言仍可审计。
- HTML实际file://离线Chromium打开：0网络请求、0断链。阅读版不是完整备份。

[RC3新证据](evidence/rc3/README.md)拥有两本全文、三次评价、原子审校、前置失败及实际三种导出。旧docs/evidence/rc2完全不改。前置失败包括另一新seed样本审校引用片段不符、原语料标题审校格式错误。后者修复了“标题失败毁掉有效正文”的缺口；最终第三次段落格式/时限问题仍保留为阻塞，不归咎于未经证实的网络故障。

### 验证命令与界限

插件 `pnpm check` 66 tests/typecheck/build、`pnpm format:check`、`git diff --check`；focused `node --test tests/narrative-v2.test.ts` 17 tests。发行 `node scripts/verify-upstream.mjs`（11239 blobs、0上游修改）、`verify-packaging-lock.mjs`（1572版本/完整性值不变）、`verify-release.mjs`。镜像构建也执行66 tests。

实际使用 `LAORENYUN_PORT=3096 docker compose -p laorenyun-rc3-final build/up -d --wait`，私密运行时配置未写入镜像。浏览器运行原 `scripts/smoke-rc2.mjs`，设置LAORENYUN_SMOKE_OUT/ORIGIN到RC3；第3次失败按脚本退出。另以本机export.mjs等待新export ID published后导出成功版本（旧链接存在时不能只等待链接可见）。私密日志保留本机，未提交访问令牌。

没有重跑腾讯云/真人录音，未改变River/Workspace/Settings/Speech/Persona架构/Main/Branch/scheduler。现有自动回归继续通过。R2 physical microphone: PENDING；R3 physical-human recovery: PENDING。

最终集成：PLUGIN锁定341091d9bd2f13a64afba28cda89acf372f2f907；镜像`laorenyun:0.2.0-rc.3` ID `sha256:e22b855b9ef303922b28be1cd3142201cd2b3ce539a286c26502074502dcd33a`。六个runtime JS（含worker）的SHA256与实测019a4ff镜像逐项相同。非root10001:10001、localhost3096、健康检查通过；实际Compose restart后新Chromium上下文能读两本成功自传及失败记录。10份新证据文件扫描：已知私密凭据匹配0、内部绝对路径匹配0。验证结束停止RC3容器，保留隔离合成卷用于后续审查；未改用户.env，未发布registry镜像或tag。
